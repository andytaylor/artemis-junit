<?xml version="1.0" encoding="UTF-8"?>
<arquillian xmlns="http://jboss.com/arquillian" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

   <defaultProtocol type="artemis remote"/>
   <group qualifier="replicated" default="true">
      <container qualifier="live1">
         <configuration>
            <property name="bootstrapHost">${live1.bootstrap.host}</property>
            <property name="bootstrapPort">${live1.bootstrap.port}</property>
            <property name="host">${live1.host}</property>
            <property name="port">${live1.port}</property>
            <property name="startTimeout">30000</property>
         </configuration>
      </container>

      <container qualifier="live2">
         <configuration>
            <property name="bootstrapHost">${live2.bootstrap.host}</property>
            <property name="bootstrapPort">${live2.bootstrap.port}</property>
            <property name="host">${live2.host}</property>
            <property name="port">${live2.port}</property>
            <property name="startTimeout">30000</property>
         </configuration>
      </container>

      <container qualifier="live3">
         <configuration>
            <property name="bootstrapHost">${live3.bootstrap.host}</property>
            <property name="bootstrapPort">${live3.bootstrap.port}</property>
            <property name="host">${live3.host}</property>
            <property name="port">${live3.port}</property>
            <property name="startTimeout">30000</property>
         </configuration>
      </container>

      <container qualifier="replica1">
         <configuration>
            <property name="bootstrapHost">${replica1.bootstrap.host}</property>
            <property name="bootstrapPort">${replica1.bootstrap.port}</property>
            <property name="host">${replica1.host}</property>
            <property name="port">${replica1.port}</property>
            <property name="startTimeout">30000</property>
         </configuration>
      </container>

      <container qualifier="replica2">
         <configuration>
            <property name="bootstrapHost">${replica2.bootstrap.host}</property>
            <property name="bootstrapPort">${replica2.bootstrap.port}</property>
            <property name="host">${replica2.host}</property>
            <property name="port">${replica2.port}</property>
            <property name="startTimeout">30000</property>
         </configuration>
      </container>

      <container qualifier="replica3">
         <configuration>
            <property name="bootstrapHost">${replica3.bootstrap.host}</property>
            <property name="bootstrapPort">${replica3.bootstrap.port}</property>
            <property name="host">${replica3.host}</property>
            <property name="port">${replica3.port}</property>
            <property name="startTimeout">30000</property>
         </configuration>
      </container>
   </group>
</arquillian>

